# 游戏化 Python 学习网站 · 需求清单与修复记录

## 功能需求状态

| 需求 | 状态 | 备注 |
|---|---|---|
| 账号注册 / 登录（JWT） | ✅ 已完成 | |
| 闯关答题（Pyodide 执行 + 断言判定） | ✅ 已完成 | 判定机制：运行用户代码 + 测试断言，比对**运行结果**，不比对代码形式 |
| 关卡课程体系 | ✅ 已完成 | 12 关 / 3 章；第一章基础语法、第二章数据结构、第三章 AI 协作（读 AI + 手搓） |
| 积分系统 | ✅ 已完成 | 通关加分；答案扣分 |
| 签到 | ✅ 已完成 | |
| 徽章系统 | ✅ 已完成 | 6 个徽章 |
| 标准答案查看 | ✅ 已完成 | 通关免费；未通关扣 5 分（首次，幂等）；独立面板不预填编辑器 |
| 深浅色主题切换 | ✅ 已完成 | localStorage 持久化 + 编辑器配色联动 |
| 报错行号精准定位 | ✅ 已完成 | 映射回编辑器真实行号 |
| AI 协作训练关卡（读懂/修复/手搓） | ✅ 已完成 | 第 9~12 关，TODO 标注【读 AI】/【手搓】 |
| 「给 AI 下指令」模拟题 | ⏳ 规划中 | 依赖读码/手搓基础 |
| 新题型（选择题/判断题） | ⏳ 规划中 | shared 增加可选字段，向后兼容 |
| 地图页按章节分组 | ⏳ 规划中 | 当前平铺展示 |

## 修复清单

| 日期 | 问题 | 根因 | 修复 | 状态 |
|---|---|---|---|---|
| 2026-08 | 报错行号误报 513/523 行 | 错误行号提取取到 Pyodide 内部 `_pyodide/_base.py` 的行号（traceback 第一帧），而非用户代码行号 | 优先匹配 `<exec>` 帧行号，取不到时取最内层帧；并传入用户代码行数把行号换算回编辑器 | ✅ 已验证 |
| 2026-08 | 第 3 关题面歧义：用户合法写法被判错 | TODO 3 未说明在哪个字符串上替换（原评论 vs 清洗后的评论），两种写法语义都成立但结果不同 | 题面改为"在清洗后的评论上，把'便宜'替换为'实惠'"；后续新关卡统一"断言只测结果、口径题面写死" | ✅ 已验证 |
| 2026-08 | 深色模式下编辑器语法高亮混色（浅色配色混入深色背景） | `syntaxHighlighting(defaultHighlightStyle)` 无 fallback，浅色高亮优先级高于 oneDark | 加 `{ fallback: true }`，深色下 oneDark 高亮优先 | ✅ 已验证 |

## 验收账号（测试用）

| 账号 | 密码 | 状态 |
|---|---|---|
| answer_final_001 | pass123456 | 12 关全部通关，积分 500，6 徽章齐全（随数据库重建可能变化） |

## 开发注意事项

- 改 shared 类型后必须 `cd shared && npm run build` 才能被前后端使用
- 表结构变更：删 `backend/playground.db` → 重启后端自动重建 → `npm run seed`
- seed 幂等可重复执行（清 levels 表 + 重置自增 ID，`id==order`，不影响 progress/积分）
- 新增关卡 = 只改 `backend/src/seed/levels.seed.ts`，业务代码零改动
- 新关卡 testCode 规范：只断言结果/函数返回值；数值用 `abs` 容差；模糊兜底值用成员断言（如 `in (0, None)`）；列表用 `sorted()` 比较
